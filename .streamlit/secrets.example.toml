# Example secrets for local dev (do not commit real secrets)
[auth]
  [auth.credentials]
    [auth.credentials.usernames.demo]
    name = "Demo User"
    email = "demo@example.com"
    # Generate a hashed password with: python -c "import streamlit_authenticator as stauth; print(stauth.Hasher(['changeme']).generate()[0])"
    password = "$2b$12$replace_with_bcrypt_hash"

  [auth.cookie]
    name = "kniteciq_auth"
    key = "replace_with_random_key"
    expiry_days = 30

# API keys
OPENAI_API_KEY = "sk-..."
